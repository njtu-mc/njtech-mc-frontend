import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state={
  website:{name:'NJTU MC'}
}

export default new Vuex.Store({
  state,
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
